export interface Post {
    id: number
    description: string
    image: string
}

export interface Profile {
    Name: string
    Picture: string
    BirthDate: Date
    email: string
    password: string
}